# mercenary-reselect-prototype

고등급 중복 용병 획득 시 `원하지 않는 결과에 대한 재선택 기회`를 제공하는 시스템 프로토타입 저장소입니다.

## Structure

- `docs/`: 시스템 정리 문서
- `prototype/`: 실행용 프로토타입
- `prompts/`: 프로젝트 전용 프롬프트
- `assets/`: 이미지/참고 자산

## Note

공용 작업 규칙과 가이드는 별도 공용 저장소 `game-planning-lab`에서 관리합니다.
